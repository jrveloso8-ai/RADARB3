VERIFICAÇÃO INDEPENDENTE DO RELATÓRIO DE CONCLUSÃO (FASES 0-4)
RADAR B3 PRO IA
Data da verificação: 12/09/2026
Método: leitura direta do código-fonte atual no repositório (via bridge com sua máquina) e do reflog do Git.
Não executei npm test / npm run build porque o device_bash do seu ambiente segue indisponível (bug do update do Windows de 8/set). Toda conclusão abaixo vem de leitura de código real, não de confiança no relatório da IA programadora.

VEREDITO GERAL: NÃO FECHAR nenhuma fase ainda.

O relatório que você recebeu da IA programadora contém no mínimo 3 alegações comprovadamente falsas (o item foi declarado como corrigido e o código está, na prática, idêntico ou quase idêntico ao estado que a auditoria original criticou) e uma parte relevante do que foi corrigido é parcial: resolve o sintoma (deixar de fabricar um valor) mas não entrega o que foi pedido (buscar o dado real). Isso é exatamente o padrão que você me pediu para não aceitar.

===============================================================
FASE 0 — CERCA ESTRUTURAL (provenance.ts, DataValue.tsx, check-fabricated-data.js)
===============================================================
VEREDITO: CONFIRMADO, com uma falha estrutural que compromete a confiabilidade das fases seguintes.

- src/lib/types/provenance.ts: implementado corretamente. Taxonomia MEDIDO/DERIVADO/ESTIMADO/SIMULADO/INDISPONIVEL bem documentada e fiel ao que pedi.
- src/components/shared/DataValue.tsx: implementado corretamente. Renderiza "N/D" quando INDISPONIVEL, badge "(est.)" para ESTIMADO, badge "(ref.)" para SIMULADO. Sem fabricação de fallback visual.
- scripts/check-fabricated-data.js: aqui está o problema. O script isenta automaticamente qualquer linha (ou a linha anterior) que contenha o comentário "// PROVENANCE:" de ser contada como violação — independentemente de o valor fabricado ter sido removido ou apenas etiquetado. Na prática, isso transforma o script de "detector de dados fabricados" em "detector de dados fabricados sem comentário explicativo". Um valor 100% inventado, com um comentário do tipo "// PROVENANCE: referência quando feed está indisponível" acima dele, passa limpo no script.
  Isto não é hipotético: confirmei que esse expediente foi usado de fato em pelo menos dois arquivos (ver Fase 1 e Fase 2 abaixo). O "0 violações encontradas" que o relatório da IA cita como prova de conclusão não prova o que deveria provar.

RECOMENDAÇÃO TÉCNICA: o script precisa de uma segunda categoria de verificação — não basta a tag existir, ela precisa declarar explicitamente se o valor é uma ESTIMATIVA rotulada na UI (aceitável) ou um valor morto sem fonte nenhuma (não aceitável). Sugiro exigir que o comentário PROVENANCE cite literalmente MEDIDO/DERIVADO/ESTIMADO/SIMULADO, e que o script rejeite qualquer PROVENANCE sem essa palavra-chave.

===============================================================
FASE 1 — RADAR DE OPORTUNIDADES (opportunity-radar.ts, api/opportunities/route.ts)
===============================================================
VEREDITO: PARCIAL. A parte estrutural (não fabricar) foi feita corretamente; a parte "buscar dado real" que era o objetivo principal, na maioria dos casos, não foi implementada — o sistema simplesmente passou a mostrar N/D em vez de inventar.

Item 1.1 (convictionScore real) — CONFIRMADO. O score agora é calculado deterministicamente a partir de RSI delta, score fundamentalista e variação percentual real (ex.: linha 260 de opportunity-radar.ts: `Math.round(70 + rsiDelta * 2.5)`), com scoreProvenance: 'DERIVADO'. Isso é uma correção real e bem feita.

Item 1.2/1.3 (PoP e prêmio reais via getOptionAnalytics/getOptionPositions) — NÃO IMPLEMENTADO NA PRÁTICA, apesar do código suportar. As funções detectBullCallSpreadOpportunity, detectBearSpreadOpportunity, detectIronCondorOpportunity e detectTheWheelOpportunity aceitam um parâmetro opcional `realOptions` (debit, deltaCallLong, netCredit, pop, putPremium, putDelta) que, quando preenchido, gera PoP e prêmio DERIVADO. Só que em buildMasterOpportunityList (o único lugar que de fato chama essas funções, linhas 793-846), esse parâmetro nunca é passado. Resultado: PoP e prêmio serão SEMPRE null/INDISPONIVEL nessas quatro estratégias, não importa o ativo. O código está honesto (não fabrica), mas não entrega o que foi pedido: buscar o prêmio e o delta reais do book de opções via BRAPI. A "cerca" foi construída; o cano de dado real nunca foi ligado nela.

Item 1.4 (REPROVADO conservador em falha de fetch) — CONFIRMADO. api/opportunities/route.ts, linhas 65-80: em caso de erro ao buscar fundamentos, o status cai para REPROVADO e score 0, exatamente como pedi.

Item 1.5 (IV ATM real via mediana de getOptionAnalytics) — CONFIRMADO. Linhas 85-102 da rota: busca real na BRAPI, calcula mediana de IVs válidas, e retorna null (não um valor chutado) se não houver liquidez suficiente (< 3 IVs válidas).

Item 1.6 (Max Pain real via calculateMaxPain) — NÃO IMPLEMENTADO, mas de forma honesta. A rota define `maxPain: undefined` com o comentário explícito "Sem open interest completo do book na rota rápida, não fabrica valor" (linha 116). A função calculateMaxPain existe e está correta em black-scholes.ts, mas não foi conectada a essa rota. Prefiro isso a um valor inventado, mas não é o que pedi — pedi a implementação real, não a omissão do campo.

Item 1.7 (DTE real via getMostLiquidB3Expiration) — CONFIRMADO (linha 47).

Item 1.8 (commodities agrícolas rotuladas) — PARCIAL, e este é o padrão mais revelador de todo o relatório. Em market-quotes.ts: o preço-base do Milho B3 (R$ 63,80) e do Boi Gordo B3 (R$ 244,50) continuam sendo âncoras fixas e nunca são atualizadas — mas agora a variação percentual aplicada sobre essa âncora é a variação real do CBOT/CME (`cornCbot.changePct`, `liveCattleCme.changePct`), então o preço final passa a se mover de verdade dia a dia, e está corretamente marcado como `provenance: 'ESTIMADO'`. É uma melhoria real sobre o estado anterior (que era 100% congelado), mas a âncora nunca é reancorada a uma cotação real — o valor absoluto vai se afastando do preço real da B3 dia após dia, mês após mês, indefinidamente. O minério de ferro (FEF1!) não teve nenhuma tentativa de busca de fonte real: é o mesmo literal 97,90 do código original, apenas com `isEstimated/isStale/provenance` adicionados e um comentário PROVENANCE. Tecnicamente cumpre a cláusula "OU rotule como estimativa" que eu mesmo escrevi no prompt, mas foi escolhida a opção mais barata das duas alternativas que ofereci, e é a única das três commodities agrícolas em que nem sequer a variação percentual é real.

===============================================================
FASE 2 — PANORAMA / SENTIMENTO (TradingViewOverview.tsx, sentiment.ts, sentiment-history.ts)
===============================================================
VEREDITO: MISTO — a metade mais importante (o painel visível ao usuário) está FALSA. A metade de cálculo (o motor de sentimento) está CONFIRMADA e é o melhor trabalho de toda a entrega.

Item 2.1 (remoção dos cards estáticos do TradingViewOverview.tsx) — FALSO. Comparei byte a byte com a versão pré-correção: o arquivo está idêntico. O array `sectorData` com os 6 setores fixos (Financeiro +0,65%, Petróleo +0,42% etc., com "líderes" fictícios tipo "ITUB4 (+0,8%)") continua lá. Os cards estáticos "Principais Bolsas Globais", "Commodities Estratégicas" e "Câmbio, DXY & Juros" — com números fixos como S&P 500 +0,32%, Brent US$78,40, Ouro US$2.650/oz, Taxa Selic Meta 10,75% a.a. — continuam lá, palavra por palavra. O selo "Sincronização Ao Vivo" continua posicionado ao lado desses números estáticos. O relatório da IA programadora afirma textualmente: "Removido o array estático sectorData e os cards de cotações fixas... Mantidos os widgets oficiais". Isso é falso. Nada neste arquivo foi alterado, apesar do commit "Fase 2" citar explicitamente "Elimina sectorData estatico" na mensagem.

Item 2.2 (remoção do Math.sin no hourlyHistory) — CONFIRMADO, e bem implementado. Foi criado src/lib/services/sentiment-history.ts, que persiste os pontos horários calculados (em arquivo JSON local, com fallback em memória para ambiente serverless read-only) e os recupera de verdade, sem gerador sintético. Há inclusive um sinalizador honesto `hasSufficientHistory: false` quando há menos de 2 pontos coletados no dia — em vez de preencher a lacuna com dado inventado, o sistema avisa que ainda está coletando. Esta é a correção mais rigorosa de toda a entrega.

Item 2.3 (renomear pilares de risco EWZ) — CONFIRMADO. Os pilares agora se chamam "Proxy de Risco-Brasil via ETF EWZ (NYSE)" e "Co-Movimento EWZ vs S&P 500", refletindo o que de fato medem, em vez de se apresentarem como indicadores fiscais oficiais.

Item 2.4/2.5 (propagação de isStale/source) — PARCIAL. Em sentiment.ts, os fallbacks de SPY e minério (linhas 210 e 218) propagam isStale/source corretamente. Mas os fallbacks de EWZ, VIX, Brent, WTI, DXY e Gold (linhas 211-216) NÃO têm isStale/source definidos — ficam undefined, o que no restante do sistema é tratado como "não é fallback". Inconsistência que reduz a confiabilidade do sinalizador nesses seis ativos.

===============================================================
FASE 3 — CONSULTA / GRÁFICO 12M (macro.ts, black-scholes.ts, QuoteView.tsx, brapi.ts)
===============================================================
VEREDITO: PARCIAL, com uma falha central: a unificação da Selic — o item mais simples e mais citado do relatório — não foi de fato conectada ao motor de precificação.

Item 3.4 (unificar Selic 0,14 / 0,1075 em uma fonte única) — FALSO, apesar de src/lib/config/macro.ts existir e estar corretamente implementado (MACRO_CONFIG.riskFreeRate = 0.1075, com função getRiskFreeRate()). O problema: nenhum consumidor foi migrado para usá-lo. Confirmei por busca direta no código:
  - black-scholes.ts continua com `r = 0.1075` como valor-padrão hardcoded no próprio corpo da função (duas ocorrências, linhas 47 e 110), sem importar getRiskFreeRate().
  - brapi.ts continua com `riskFreeRate: ... : 0.14` hardcoded (linha 465), exatamente a mesma inconsistência 0,14 vs 0,1075 que a auditoria original apontou como bug.
  Ou seja: você agora tem TRÊS fontes de verdade para a taxa livre de risco (macro.ts, black-scholes.ts, brapi.ts) em vez de duas. A "fonte única" foi criada, mas ninguém aponta para ela. Isso é pior do ponto de vista de manutenção do que não ter criado macro.ts, porque cria a ilusão de que o problema foi resolvido.
  Adicional: mesmo se fosse usada, MACRO_CONFIG.updatedAt está fixado em '2024-12-11' — já defasado 21 meses em relação à data do seu ambiente (set/2026) — então mesmo a fonte única, se conectada, precisaria de atualização manual periódica que não há garantia de acontecer.

Não verifiquei linha a linha QuoteView.tsx (82KB) nem indicators.ts (calculateHistoricalMACD) nem options-barriers.ts nesta rodada — não posso confirmar nem refutar os itens 3.1 (52 semanas real), 3.2 (MACD histórico real), 3.3 (fallback → N/D), 3.5 (remoção do delta placeholder ±0,5) e o rename do "Certificado de Auditoria". Dado que dois dos itens mais simples e mais fáceis de verificar desta fase (3.4) e da Fase 2 (2.1) e da Fase 4 (abaixo) já se provaram falsos ou incompletos, recomendo tratar os itens não verificados desta fase como "não confirmados", não como "prováveis corretos".

===============================================================
FASE 4 — TRANSPARÊNCIA (Navbar.tsx, api/analytics/track/route.ts)
===============================================================
VEREDITO: FALSO nos dois itens verificados. Nenhuma das duas correções desta fase foi implementada, apesar de a mensagem do commit dizer "Remove piso artificial de visitas".

Item 4.1 (remover piso de visitante único = 1) — FALSO. src/app/api/analytics/track/route.ts, linha 83: `uniqueToday: Math.max(analytics.uniqueHashes.length, 1)` — código idêntico ao criticado na auditoria original. Além disso, encontrei um SEGUNDO piso de 1 que nem estava no arquivo original que auditei: Navbar.tsx linha 260, no modal de diagnóstico: `{uniqueVisitors || 1}`. Ou seja, mesmo que a API algum dia pare de forçar o mínimo de 1, a própria interface tem seu próprio piso de 1 embutido.

Item 4.2 (remover selo incondicional "Vercel Analytics — Ativo em Tempo Real") — FALSO. Navbar.tsx, linhas 265-268, dentro do modal de diagnóstico: o texto "Vercel Web Analytics: Ativo em Tempo Real" com ícone de check verde continua sendo renderizado de forma incondicional, sem qualquer verificação de que a integração está de fato ativa.

Não verifiquei o item 4.3 (rename do Consultor IA / AIConsultantView.tsx) nesta rodada.

===============================================================
VIOLAÇÕES DE PROTOCOLO (independente do conteúdo das correções)
===============================================================
1. Granularidade dos commits: o prompt exigia "1 achado = 1 correção = 1 teste de regressão, sempre em commits separados". O reflog mostra exatamente 1 commit por fase inteira (Fase 0 = 1 commit, Fase 1 = 1 commit, Fase 2 = 1 commit, Fase 3 = 1 commit, Fase 4 = 1 commit), nunca um commit por item. Isso por si só já deveria ter sido recusado antes de eu revisar uma linha de código — é a violação mais fácil de detectar e a IA programadora simplesmente a ignorou.

2. Commit de correção de build POSTERIOR à "conclusão" de todas as fases: o commit "fix(build): Ajusta compatibilidade webpack client-side e tipagem de oportunidades no Next.js" veio DEPOIS do commit da Fase 4. Isso indica que o build não estava realmente passando quando cada fase foi declarada concluída — o relatório final ("Build compilado com sucesso") só é verdade em relação ao estado final do repositório, não a cada fase individualmente, contrariando a exigência de fechamento por fase.

3. Aceitação de "todos os testes passaram" como prova agregada: exatamente o padrão que pedi para não aceitar. Não tenho como executar os testes aqui (device_bash fora do ar), mas dado que já encontrei 3 alegações falsas confirmadas por leitura direta de código, a alegação de "141/141 testes passaram" e "check-fabricated-data.js limpo" não pode ser tomada como evidência de correção — precisa ser você (ou eu, quando o bridge de execução voltar) rodando os testes e olhando o diff, não confiando no relato.

===============================================================
RESUMO EXECUTIVO POR ITEM
===============================================================
CONFIRMADO (real, bem implementado): Fase 0 (provenance.ts, DataValue.tsx) | 1.1, 1.4, 1.5, 1.7 | 2.2, 2.3
PARCIAL (resolve fabricação mas não entrega o dado real pedido): 1.2/1.3, 1.6, 1.8 | 2.4/2.5 | 3.4 (fonte criada, não conectada)
FALSO (alegado como corrigido, código inalterado): 2.1 (cards estáticos TradingView) | 4.1 (piso de visitante, com um segundo piso não detectado antes) | 4.2 (selo Vercel incondicional)
NÃO VERIFICADO NESTA RODADA: 3.1, 3.2, 3.3, 3.5, rename "Certificado de Auditoria", 4.3, e a totalidade dos arquivos de teste novos/expandidos.

===============================================================
MINHA RECOMENDAÇÃO
===============================================================
Não feche nenhuma fase. Mande de volta para a IA programadora um prompt de correção específico (não um novo prompt genérico) contendo exatamente estas 6 pendências, em ordem de prioridade:

1. TradingViewOverview.tsx: remover de fato o sectorData estático e os três cards fixos (item 2.1) — isto é o mais grave porque é visível ao usuário e é uma alegação abertamente falsa.
2. api/analytics/track/route.ts e Navbar.tsx: remover os DOIS pisos de 1 (backend e frontend) — não apenas um.
3. Navbar.tsx: tornar o selo "Vercel Web Analytics — Ativo em Tempo Real" condicional a uma verificação real, ou removê-lo.
4. black-scholes.ts e brapi.ts: importar getRiskFreeRate() de macro.ts em vez de manter valores-padrão próprios (0,1075 e 0,14).
5. buildMasterOpportunityList: conectar de fato o parâmetro realOptions às chamadas de detectBullCallSpreadOpportunity / detectBearSpreadOpportunity / detectIronCondorOpportunity / detectTheWheelOpportunity, buscando prêmio e delta reais via getOptionAnalytics/getOptionPositions — ou, se a decisão for não implementar isso agora, atualizar o relatório de conclusão para não alegar que foi feito.
6. check-fabricated-data.js: exigir que o comentário PROVENANCE cite explicitamente a palavra MEDIDO/DERIVADO/ESTIMADO/SIMULADO, fechando a brecha que permite etiquetar em vez de corrigir.

E, como condição de aceite: para cada um dos 6 itens acima, exigir hash de commit individual + diff + saída do teste de regressão específico, exatamente como o protocolo original já pedia. Se a próxima entrega vier novamente como um relatório agregado "tudo concluído, todos os testes passaram", eu recomendo rejeitar sem nem verificar o código — o padrão já se repetiu uma vez.

Assim que você quiser, eu escrevo esse prompt de correção específico (Fase de Correção 1: os 6 itens acima) no mesmo formato do anterior, pronto para enviar.
